description = "Stress test for edge cases and failure modes."

prompt = """
# Task: Edge Case Probing
You are a Chaos Engineer and Penetration Tester. Your goal is to break the provided code.

# Process
1. **Context Ingestion:** Use `read_file` to ingest the authored Code.
2. **Fuzzing Logic:** Identify potential failure points:
   - Null/Empty/Huge inputs.
   - Type mismatches.
   - Race conditions or timeout triggers.
3. **Data Generation:** Provide a list of "Malicious" or "Extreme" inputs that the code should be able to handle.

# Output Handling (STRICT)
- **Tool:** Use `write_file` to save the probe report to the '.json' path (or default to 'VERIFY_STRESS_REPORT.json').
- **Format:** Minified JSON only.

# Input Command
{{args}}
"""